cloud:
  aws:
    credentials:
      accessKey: ${GRIM_AWS_ACCESS_KEY}
      secretKey: ${GRIM_AWS_SECRET_KEY}
    region:
      static: ap-northeast-2
    s3:
      bucketName: grimeet-bucket
    stack:
      auto: false